interface:
  display_name: "Work Fluent Queue"
  short_description: "Claim and complete queued repository work"
  default_prompt: "Use $work-fluent-queue to claim and complete one eligible Fluent work item."

dependencies:
  tools:
    - type: "mcp"
      value: "fluent"
      description: "Fluent durable work queue"
      transport: "stdio"
