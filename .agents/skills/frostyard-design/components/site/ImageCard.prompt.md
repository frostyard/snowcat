Product card: mono kicker + 0N, full-bleed crop of the alpine hero art (never redrawn mountains), lowercase name at 2rem, tags row.
```jsx
<ImageCard type="Desktop" index={0} name="snow" title="A GNOME workstation that stays composed." body="A daily desktop built on Debian Trixie…" tags={["GNOME","Backports kernel","Atomic updates"]} />
```
Default scenes by index: moonlit-summit, alpine-morning, frozen-reflection, blueprint. Pass `assetsBase` if the page isn't two levels below the project root. 3-up grid, 1rem gap, min-height 425px.