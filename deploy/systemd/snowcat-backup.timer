# Daily backup; Persistent=true runs a missed backup at the next boot. Enable
# with `systemctl enable --now snowcat-backup.timer`.
[Unit]
Description=Snowcat: back up the databases daily

[Timer]
OnCalendar=daily
Persistent=true
Unit=snowcat-backup.service

[Install]
WantedBy=timers.target
