# Artifact verification and the pull-request cure sweep every 10 minutes.
# Enable with `systemctl enable --now snowcat-verify.timer`.
[Unit]
Description=Snowcat: refresh artifact verification every 10 minutes

[Timer]
OnCalendar=*:0/10
Unit=snowcat-verify.service

[Install]
WantedBy=timers.target
