# Artifact verification every 15 minutes. Enable with
# `systemctl enable --now fluent-verify.timer`.
[Unit]
Description=Fluent: refresh artifact verification every 15 minutes

[Timer]
OnCalendar=*:0/15
Unit=fluent-verify.service

[Install]
WantedBy=timers.target
